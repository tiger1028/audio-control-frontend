const SERVER_PROTOCOL = "http";
const SERVER_URL = "localhost:8080";

export const SERVER_BASE_URL = `${SERVER_PROTOCOL}://${SERVER_URL}`;
export const SERVER_API_URL = `${SERVER_PROTOCOL}://${SERVER_URL}/api/v1`;
